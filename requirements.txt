# requirements-docker.txt
# Cleaned runtime dependencies for ARS-GUI on Python 3.10 (Docker)
# - Removed duplicate/conflicting IPython pins
# - Dropped stdlib backports (ConfigParser, HTMLParser)
# - Lean pins chosen for widewheel availability on linux/amd64
# - Includes TensorFlow CPU for TFLite via tensorflow.lite; if your code uses
#   tflite_runtime.Interpreter instead, you can replace tensorflow with tflite-runtime.

# Core scientific stack
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
Pillow==10.4.0

# ML / feature extraction / clustering
scikit-learn==1.3.2
umap-learn==0.5.6
numba==0.59.1
llvmlite==0.42.0

# TensorFlow CPU (includes tensorflow.lite)
tensorflow==2.16.1

# GUI & utils
customtkinter==5.2.2
opencv-python==4.9.0.80
tqdm==4.66.4
colorama==0.4.6
filelock==3.12.4

# EfficientNet (if you load efficientnetb3_notop.h5 via this package)
efficientnet==1.1.1

# Optional dev tool (safe to remove in production)
ipython==8.30.0
